<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ClubRecipee</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <style>
        /* Custom colors */
            body {
                background-color: #2c3e50; /* Dark blue background */
                color: #ecf0f1; /* Light text color */
                font-family: 'Poppins', sans-serif; /* Custom font */
            }
    
            h1, h2, h3, h4, h5, h6 {
                color: gold; /* Tomato red for headings */
            }
    
            a {
                color: #3498db; /* Light blue for links */
            }
    
            a:hover {
                color: #2980b9; /* Darker blue for hover state */
            }
    
            .btn-primary {
                background-color: #3498db; /* Tomato red for primary buttons #9de73c*/ 
                border-color: #3498db;
            }
    
            .btn-primary:hover {
                background-color: #2b46c0; /* Darker red for hover state */
                border-color: #2b46c0;
            }
    
            .btn-danger {
                background-color: #e74c3c; /* Tomato red for danger buttons */
                border-color: #e74c3c;
            }
    
            .btn-danger:hover {
                background-color: #c0392b; /* Darker red for hover state */
                border-color: #c0392b;
            }
            .club-text {
                color: gold;
                /* background-color: #7CFC00; Light green background */
                /* padding: 2px 5px; */
                border-radius: 3px;
            }

            .recipee-text {
                color: #7CFC00; /* Light green text color */
                /* background-color: gold; */
                /* padding: 2px 5px; */
                border-radius: 3px;
            }
                /* Custom lighter green text color */
            .text-light-green {
                color: #7CFC00; /* Light green color */
            }
     </style>
</head>
<body class="d-flex flex-column bg-dark text-light vh-100">
    <%- include('../partials/navbar')%>
    <main class="container mt-5">
        <%- body %>
    </main>
    <%- include('../partials/footer')%>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
    <script>
        // Example starter JavaScript for disabling form submissions if there are invalid fields
        (function () {
            'use strict'
            
            // Fetch all the forms we want to apply custom Bootstrap validation styles to
            const forms = document.querySelectorAll('.validate-form')
            
            // Loop over them and prevent submission
            Array.from(forms)
                .forEach(function (form) {
                form.addEventListener('submit', function (event) {
                    if (!form.checkValidity()) {
                    event.preventDefault()
                    event.stopPropagation()
                    }
            
                    form.classList.add('was-validated')
                }, false)
                })
            })();
    </script>
</body>
</html>